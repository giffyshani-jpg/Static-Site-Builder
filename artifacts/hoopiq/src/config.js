// Central app configuration.
// Swap API_BASE_URL to point at a real backend when one becomes available.
export const API_BASE_URL =
  import.meta.env.VITE_API_BASE_URL || "https://api.hoopiq.app/v1";
